*.mp4 filter=lfs diff=lfs merge=lfs -text
# Hero video is served on the web (Vercel), keep it as a regular file so it is
# not stored in LFS (Vercel does not resolve LFS pointers for static assets).
public/hero-merged.mp4 !filter !diff !merge -text
